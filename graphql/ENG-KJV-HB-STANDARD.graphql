
type Verse {
  book: String!
  chapter: Int!
  verse: Int!
  text: String!
}

type Bible {
  id: ID!
  version: String!
  verses: [Verse!]!
}

type Query {
  verse(book: String!, chapter: Int!, verse: Int!): Verse
  versesByBook(book: String!): [Verse!]!
  search(query: String!): [Verse!]!
}
